# Data files

These JSON files are the live "database" for the app. The admin UI reads and writes them via the GitHub Contents API.

**Don't hand-edit.** Use the app — it keeps IDs, timestamps, and cross-references consistent. If you do need to edit manually, be careful to preserve the JSON array shape and keep `id` fields unique.
